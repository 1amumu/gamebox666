(() => {
  const PROFILE_MAP = {
    "10470001344450": {
      name: "kraken_850",
      status: "在线",
      statusClass: "online",
      tag: "低端玩家",
      tagClass: "tag-low",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "0.00",
      todayBet: "327,270.00",
      historyBet: "589,086.00",
      todayRtp: "65.28%",
      historyRtp: "32.50%",
      todayWin: "-113,623.40",
      historyWin: "-113,623.40",
      todayRounds: "0",
      historyRounds: "6",
      loginCount: "0",
      retentionDays: "1",
      createdAt: "2026-04-24 13:16:50",
      lastLogin: "2026-05-07 14:05:32",
      registerIps: ["103.48.12.16", "103.48.12.27", "103.48.13.04"],
      loginIps: ["103.48.12.16", "103.48.12.21"]
    },
    "10470007305040": {
      name: "vip_royal",
      status: "在线",
      statusClass: "online",
      tag: "优质玩家",
      tagClass: "tag-premium",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "90.16",
      todayBet: "428,720.00",
      historyBet: "802,316.00",
      todayRtp: "91.40%",
      historyRtp: "103.20%",
      todayWin: "246,318.52",
      historyWin: "246,318.52",
      todayRounds: "3",
      historyRounds: "12",
      loginCount: "3",
      retentionDays: "2",
      createdAt: "2026-04-23 11:05:12",
      lastLogin: "2026-05-07 13:52:18",
      registerIps: ["82.14.66.120", "82.14.66.153"],
      loginIps: ["82.14.66.153"]
    },
    "10470004414669": {
      name: "gems_4469",
      status: "离线",
      statusClass: "offline",
      tag: "中端玩家",
      tagClass: "tag-mid",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "400.00",
      todayBet: "161,024.00",
      historyBet: "185,244.00",
      todayRtp: "102.80%",
      historyRtp: "94.40%",
      todayWin: "-24,220.90",
      historyWin: "-24,220.90",
      todayRounds: "0",
      historyRounds: "8",
      loginCount: "0",
      retentionDays: "2",
      createdAt: "2026-04-21 08:32:19",
      lastLogin: "2026-05-06 21:44:09",
      registerIps: ["61.177.92.44", "61.177.92.58", "61.177.92.61"],
      loginIps: ["61.177.92.61", "61.177.92.78"]
    },
    "10470004889926": {
      name: "fast_score",
      status: "风险",
      statusClass: "risk",
      tag: "疑似刷分",
      tagClass: "tag-risk",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "109.12",
      todayBet: "202,265.06",
      historyBet: "260,915.19",
      todayRtp: "0.00%",
      historyRtp: "0.00%",
      todayWin: "-30,650.13",
      historyWin: "-30,650.13",
      todayRounds: "8",
      historyRounds: "10",
      loginCount: "8",
      retentionDays: "1",
      createdAt: "2026-04-30 12:15:44",
      lastLogin: "2026-05-07 12:18:45",
      registerIps: ["45.116.228.19", "45.116.228.33"],
      loginIps: ["45.116.228.33"]
    },
    "10410001538841": {
      name: "maya_blue",
      status: "在线",
      statusClass: "online",
      tag: "优质玩家",
      tagClass: "tag-premium",
      merchant: "ar518888",
      merchantId: "104100",
      balance: "1,280.00",
      todayBet: "161,662.16",
      historyBet: "516,882.00",
      todayRtp: "120.78%",
      historyRtp: "120.78%",
      todayWin: "82,441.20",
      historyWin: "82,441.20",
      todayRounds: "2",
      historyRounds: "16",
      loginCount: "2",
      retentionDays: "2",
      createdAt: "2026-04-18 17:28:36",
      lastLogin: "2026-05-07 11:08:20",
      registerIps: ["119.28.74.90", "119.28.74.116", "119.28.75.02"],
      loginIps: ["119.28.75.02", "119.28.75.18"]
    },
    "10470000804108": {
      name: "mid_4108",
      status: "离线",
      statusClass: "offline",
      tag: "中端玩家",
      tagClass: "tag-mid",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "0.16",
      todayBet: "210,356.00",
      historyBet: "324,814.92",
      todayRtp: "82.07%",
      historyRtp: "82.07%",
      todayWin: "-14,458.92",
      historyWin: "-14,458.92",
      todayRounds: "0",
      historyRounds: "7",
      loginCount: "0",
      retentionDays: "2",
      createdAt: "2026-04-27 14:02:48",
      lastLogin: "2026-05-06 18:26:59",
      registerIps: ["88.214.201.13", "88.214.201.26"],
      loginIps: ["88.214.201.26"]
    },
    "10470001601623": {
      name: "locked_1623",
      status: "冻结",
      statusClass: "locked",
      tag: "疑似刷分",
      tagClass: "tag-risk",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "0.00",
      todayBet: "268,073.00",
      historyBet: "412,558.40",
      todayRtp: "93.10%",
      historyRtp: "95.93%",
      todayWin: "-16,803.67",
      historyWin: "-16,803.67",
      todayRounds: "0",
      historyRounds: "9",
      loginCount: "0",
      retentionDays: "1",
      createdAt: "2026-04-20 10:35:44",
      lastLogin: "2026-05-06 10:35:44",
      registerIps: ["38.54.102.17"],
      loginIps: ["38.54.102.17"]
    },
    "10470001978708": {
      name: "low_8708",
      status: "在线",
      statusClass: "online",
      tag: "低端玩家",
      tagClass: "tag-low",
      merchant: "ar518888",
      merchantId: "104700",
      balance: "56.80",
      todayBet: "64,750.00",
      historyBet: "118,240.00",
      todayRtp: "74.90%",
      historyRtp: "82.00%",
      todayWin: "-21,312.70",
      historyWin: "-21,312.70",
      todayRounds: "1",
      historyRounds: "5",
      loginCount: "1",
      retentionDays: "1",
      createdAt: "2026-04-28 09:55:02",
      lastLogin: "2026-05-07 09:55:02",
      registerIps: ["14.18.66.205"],
      loginIps: ["14.18.66.205"]
    }
  };

  const BET_RECORDS = {
    "10470001344450": [
      { brand: "PG", orderId: "GB202605110001", roundId: "RPG-61148290", gameCn: "海怪之力", gameEn: "PowerOfTheKraken", trigger: "普通投注", rtp: "96.20%", multiple: "1.20x", bet: "8,420.00", win: "1,700.00", before: "54,880.00", after: "56,580.00", date: "2026-05-11", time: "13:48:22" },
      { brand: "PG", orderId: "GB202605100087", roundId: "RPG-61030087", gameCn: "海怪之力", gameEn: "PowerOfTheKraken", trigger: "普通投注", rtp: "98.36%", multiple: "1.08x", bet: "4,200.00", win: "352.00", before: "50,328.00", after: "50,680.00", date: "2026-05-10", time: "21:12:06" },
      { brand: "PP", orderId: "GB202605090142", roundId: "RPP-60910142", gameCn: "法老牌", gameEn: "Pharaoh Cards", trigger: "免费旋转", rtp: "88.10%", multiple: "0.76x", bet: "5,600.00", win: "-1,064.00", before: "47,990.00", after: "46,926.00", date: "2026-05-09", time: "10:26:48" }
    ],
    "10470007305040": [
      { brand: "PG", orderId: "GB202605110002", roundId: "RPG-61148291", gameCn: "海怪传说", gameEn: "eKraken", trigger: "免费旋转", rtp: "91.40%", multiple: "0.55x", bet: "16,800.00", win: "-7,560.00", before: "132,640.00", after: "125,080.00", date: "2026-05-11", time: "12:21:09" },
      { brand: "JILI", orderId: "GB202605100064", roundId: "RJL-61012064", gameCn: "龙之宝藏", gameEn: "Dragon Treasure", trigger: "普通投注", rtp: "103.20%", multiple: "1.64x", bet: "12,400.00", win: "7,120.00", before: "118,960.00", after: "126,080.00", date: "2026-05-10", time: "17:08:14" }
    ],
    "10470004414669": [
      { brand: "PG", orderId: "GB202605100128", roundId: "RPG-61039128", gameCn: "宝石财富", gameEn: "Fortune Gems", trigger: "普通投注", rtp: "102.80%", multiple: "1.52x", bet: "3,600.00", win: "1,888.00", before: "26,300.00", after: "28,188.00", date: "2026-05-10", time: "20:06:13" },
      { brand: "SPRLBE", orderId: "GB202605090033", roundId: "RSP-60900033", gameCn: "幸运糖果", gameEn: "Lucky Candy", trigger: "普通投注", rtp: "94.40%", multiple: "0.92x", bet: "2,240.00", win: "-128.00", before: "28,316.00", after: "28,188.00", date: "2026-05-09", time: "09:16:55" }
    ],
    "10470004889926": [
      { brand: "JILI", orderId: "GB202605090276", roundId: "RPG-60988276", gameCn: "玛雅黄金", gameEn: "Maya Gold", trigger: "普通投注", rtp: "0.00%", multiple: "0.00x", bet: "22,000.00", win: "-22,000.00", before: "18,960.00", after: "-3,040.00", date: "2026-05-09", time: "18:44:30" }
    ],
    "10410001538841": [
      { brand: "PP", orderId: "GB202605080318", roundId: "RPG-60855318", gameCn: "保龄球之王", gameEn: "Bowling King", trigger: "普通投注", rtp: "120.78%", multiple: "1.21x", bet: "11,260.00", win: "2,340.00", before: "146,520.00", after: "148,860.00", date: "2026-05-08", time: "10:32:45" },
      { brand: "JILI", orderId: "GB202605070148", roundId: "RJL-60710148", gameCn: "玛雅黄金", gameEn: "Maya Gold", trigger: "免费旋转", rtp: "87.00%", multiple: "0.60x", bet: "6,840.00", win: "-2,736.00", before: "151,596.00", after: "148,860.00", date: "2026-05-07", time: "16:42:11" }
    ],
    "10470000804108": [
      { brand: "SPRLBE", orderId: "GB202605070536", roundId: "RPG-60770536", gameCn: "幸运糖果", gameEn: "Lucky Candy", trigger: "免费旋转", rtp: "82.07%", multiple: "0.82x", bet: "7,920.00", win: "-1,420.00", before: "33,680.00", after: "32,260.00", date: "2026-05-07", time: "23:12:18" }
    ]
  };

  const SAME_DEVICE_MAP = {
    "10470001344450": [
      { id: "10470001344450", registeredAt: "2026-04-24 13:16:50", lastLogin: "2026-05-07 14:05:32", totalBet: "589,086.00", totalPayout: "475,462.60", historyProfit: "-113,623.40" },
      { id: "10470001344512", registeredAt: "2026-04-26 09:42:18", lastLogin: "2026-05-06 21:18:44", totalBet: "226,440.00", totalPayout: "201,118.00", historyProfit: "-25,322.00" }
    ],
    "10470007305040": [
      { id: "10470007305040", registeredAt: "2026-04-23 11:05:12", lastLogin: "2026-05-07 13:52:18", totalBet: "802,316.00", totalPayout: "1,048,634.52", historyProfit: "246,318.52" }
    ],
    "10470004414669": [
      { id: "10470004414669", registeredAt: "2026-04-21 08:32:19", lastLogin: "2026-05-06 21:44:09", totalBet: "185,244.00", totalPayout: "161,023.10", historyProfit: "-24,220.90" },
      { id: "10470004414702", registeredAt: "2026-04-29 15:08:51", lastLogin: "2026-05-05 10:26:17", totalBet: "92,600.00", totalPayout: "88,412.00", historyProfit: "-4,188.00" }
    ],
    "10470004889926": [
      { id: "10470004889926", registeredAt: "2026-04-30 12:15:44", lastLogin: "2026-05-07 12:18:45", totalBet: "260,915.19", totalPayout: "230,265.06", historyProfit: "-30,650.13" }
    ],
    "10410001538841": [
      { id: "10410001538841", registeredAt: "2026-04-18 17:28:36", lastLogin: "2026-05-07 11:08:20", totalBet: "516,882.00", totalPayout: "599,323.20", historyProfit: "82,441.20" },
      { id: "10410001538877", registeredAt: "2026-04-25 09:31:02", lastLogin: "2026-05-04 19:52:41", totalBet: "144,880.00", totalPayout: "138,520.00", historyProfit: "-6,360.00" }
    ],
    "10470000804108": [
      { id: "10470000804108", registeredAt: "2026-04-27 14:02:48", lastLogin: "2026-05-06 18:26:59", totalBet: "324,814.92", totalPayout: "310,356.00", historyProfit: "-14,458.92" }
    ]
  };

  const GAME_ICON_MAP = {
    PowerOfTheKraken: ["kraken", "K"],
    eKraken: ["ek", "eK"],
    "Fortune Gems": ["gems", "FG"],
    "Pharaoh Cards": ["pharaoh", "法"],
    "Bowling King": ["bowling", "保"],
    "Maya Gold": ["maya", "玛"],
    "Dragon Treasure": ["dragon", "龙"],
    "Lucky Candy": ["candy", "糖"],
    Avlator: ["kraken", "A"]
  };

  const state = {
    modal: null,
    currentPlatformId: "",
    currentSource: null
  };

  function moneyNumber(text) {
    const value = Number(String(text || "").replace(/,/g, ""));
    return Number.isFinite(value) ? value : 0;
  }

  function formatNumber(value) {
    return Number(value).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  }

  function bindCopyButtons(scope = document) {
    scope.querySelectorAll("[data-copy]").forEach((button) => {
      if (button.dataset.copyBound === "true") return;
      button.dataset.copyBound = "true";
      button.addEventListener("click", async (event) => {
        event.stopPropagation();
        const value = button.dataset.copy || "";
        try {
          await navigator.clipboard.writeText(value);
        } catch (_) {
          const textarea = document.createElement("textarea");
          textarea.value = value;
          document.body.appendChild(textarea);
          textarea.select();
          document.execCommand("copy");
          textarea.remove();
        }
        button.classList.add("copied");
        setTimeout(() => button.classList.remove("copied"), 900);
      });
    });
  }

  function hoverListHtml(items, triggerLabel) {
    if (!items || !items.length) return "-";
    if (items.length === 1) return items[0];
    return `
      <span class="detail-hover-group">
        <button class="detail-link-btn detail-hover-trigger" type="button">${triggerLabel}</button>
        <span class="detail-hover-panel">
          ${items.map((item) => `
            <span class="detail-hover-item">
              <i>${item}</i>
              <button class="profit-copy" type="button" data-copy="${item}" title="复制IP" aria-label="复制IP"></button>
            </span>
          `).join("")}
        </span>
      </span>
    `;
  }

  function playerTagHtml(profile) {
    if (!profile.tag) return "-";
    return `<span class="player-tag ${profile.tagClass || "tag-low"}">${profile.tag}</span>`;
  }

  function statusHtml(profile) {
    if (!profile.status) return "-";
    return `<span class="account-status ${profile.statusClass || ""}">${profile.status}</span>`;
  }

  function summarizeSource(platformId, row) {
    const profile = PROFILE_MAP[platformId] || {};
    const base = {
      platformId,
      gameCn: "",
      gameEn: "",
      brand: "",
      bet: profile.todayBet || "0.00",
      win: profile.todayWin || "0.00",
      count: profile.todayRounds || "0",
      rtp: profile.todayRtp || "0.00%",
      createdAt: profile.lastLogin || "-"
    };
    if (!row) return { profile, ...base };
    if (row.matches(".profit-row[data-platform]")) {
      const bet = row.dataset.bet || row.children[4]?.innerText.trim() || base.bet;
      const payout = row.dataset.payout || row.children[5]?.innerText.trim() || "0.00";
      const win = row.dataset.win || row.children[6]?.innerText.trim() || base.win;
      const rtp = moneyNumber(bet) ? `${((moneyNumber(payout) / moneyNumber(bet)) * 100).toFixed(2)}%` : "0.00%";
      return {
        profile,
        platformId,
        gameCn: row.dataset.game || "",
        gameEn: row.dataset.game || "",
        brand: row.dataset.brand || "",
        bet,
        win,
        count: row.dataset.count || row.children[3]?.innerText.trim() || base.count,
        rtp,
        createdAt: row.dataset.date || row.children[2]?.innerText.trim() || base.createdAt
      };
    }
    if (row.matches(".game-record-row[data-player]")) {
      return {
        profile,
        platformId,
        gameCn: row.children[4]?.innerText.trim() || "",
        gameEn: row.children[5]?.innerText.trim() || "",
        brand: row.dataset.brand || "",
        bet: row.children[9]?.innerText.trim() || base.bet,
        win: row.children[10]?.innerText.trim() || base.win,
        count: "1",
        rtp: row.children[7]?.innerText.trim() || base.rtp,
        createdAt: row.children[13]?.innerText.trim() || base.createdAt
      };
    }
    return { profile, ...base };
  }

  function ensureModal() {
    if (state.modal) return state.modal;
    const wrapper = document.createElement("div");
    wrapper.innerHTML = `
      <div class="account-detail-mask" data-shared-account-detail hidden>
        <div class="account-detail-dialog" role="dialog" aria-modal="true" aria-labelledby="shared-account-detail-title">
          <div class="account-detail-head">
            <div>
              <h3 id="shared-account-detail-title">会员详情 <span data-detail-id></span></h3>
              <p>按当前列表数据同步展示会员基础信息与游戏输赢概览。</p>
            </div>
            <div class="account-detail-head-actions">
              <button class="detail-expand-btn" type="button" data-detail-fullscreen>全屏</button>
              <button class="account-detail-close" type="button" data-detail-close aria-label="关闭">×</button>
            </div>
          </div>
          <div class="account-detail-body">
            <div class="account-detail-grid" data-detail-grid></div>
            <div class="account-detail-tabs" data-detail-tabs>
              <button class="active" type="button" data-detail-tab="game">游戏输赢数据</button>
              <button type="button" data-detail-tab="bet">投注行为</button>
              <button type="button" data-detail-tab="device">同设备账号</button>
            </div>
            <div class="account-detail-panel active" data-detail-panel="game">
              <div class="account-detail-table-wrap">
                <div class="account-detail-table" data-detail-game-table></div>
              </div>
            </div>
            <div class="account-detail-panel" data-detail-panel="bet">
              <div class="account-detail-filter bet-record-filter">
                <label><span>游戏</span><select class="account-input account-select" data-bet-game><option value="">全部</option></select></label>
                <label><span>开始</span><input class="profit-input date" type="date" value="2026-05-06" data-bet-from /></label>
                <span class="profit-date-range-sep">至</span>
                <label><input class="profit-input date" type="date" value="2026-05-11" data-bet-to aria-label="投注行为结束日期" /></label>
                <div>
                  <button class="profit-btn" type="button" data-bet-reset>重置</button>
                  <button class="profit-btn primary" type="button" data-bet-search>搜索</button>
                </div>
              </div>
              <div class="game-record-table-wrap account-bet-table-wrap">
                <div class="game-record-table account-bet-table" data-bet-table></div>
              </div>
              <div class="account-detail-summary" data-bet-summary>共 0 条记录</div>
            </div>
            <div class="account-detail-panel" data-detail-panel="device">
              <div class="account-detail-table-wrap">
                <div class="account-detail-table same-device-table" data-device-table></div>
              </div>
              <div class="account-detail-summary" data-device-summary>共 0 条记录</div>
            </div>
          </div>
        </div>
      </div>
    `;
    document.body.appendChild(wrapper.firstElementChild);
    state.modal = document.querySelector("[data-shared-account-detail]");
    const dialog = state.modal.querySelector(".account-detail-dialog");
    const detailGrid = state.modal.querySelector("[data-detail-grid]");
    const detailId = state.modal.querySelector("[data-detail-id]");
    const detailGameTable = state.modal.querySelector("[data-detail-game-table]");
    const betGame = state.modal.querySelector("[data-bet-game]");
    const betFrom = state.modal.querySelector("[data-bet-from]");
    const betTo = state.modal.querySelector("[data-bet-to]");
    const betTable = state.modal.querySelector("[data-bet-table]");
    const betSummary = state.modal.querySelector("[data-bet-summary]");
    const deviceTable = state.modal.querySelector("[data-device-table]");
    const deviceSummary = state.modal.querySelector("[data-device-summary]");
    const detailTabs = Array.from(state.modal.querySelectorAll("[data-detail-tab]"));
    const detailPanels = Array.from(state.modal.querySelectorAll("[data-detail-panel]"));
    const fullscreenButton = state.modal.querySelector("[data-detail-fullscreen]");

    function switchTab(tabName) {
      detailTabs.forEach((item) => item.classList.toggle("active", item.dataset.detailTab === tabName));
      detailPanels.forEach((panel) => panel.classList.toggle("active", panel.dataset.detailPanel === tabName));
    }

    function syncBetGames(platformId) {
      const current = betGame.value;
      const games = Array.from(new Set((BET_RECORDS[platformId] || []).map((item) => item.gameEn)));
      betGame.innerHTML = `<option value="">全部</option>${games.map((game) => `<option value="${game}">${game}</option>`).join("")}`;
      betGame.value = games.includes(current) ? current : "";
    }

    function renderBetRecords(platformId) {
      const records = (BET_RECORDS[platformId] || []).filter((item) => {
        const matchGame = !betGame.value || item.gameEn === betGame.value;
        const matchFrom = !betFrom.value || item.date >= betFrom.value;
        const matchTo = !betTo.value || item.date <= betTo.value;
        return matchGame && matchFrom && matchTo;
      });
      betSummary.textContent = `共 ${records.length} 条记录`;
      betTable.innerHTML = `
        <div class="game-record-row header">
          <div>序号</div><div>订单编号</div><div>牌局编号</div><div>中文游戏名称</div><div>英文游戏名称</div><div>触发方式</div><div>RTP</div><div>返奖倍数</div><div>投注金额</div><div>玩家输赢</div><div>投注前余额</div><div>结算后余额</div><div>创建时间</div>
        </div>
        ${records.map((item, index) => `
          <div class="game-record-row">
            <div>${index + 1}</div>
            <div><span class="record-copy-value">${item.orderId} <button class="profit-copy" type="button" data-copy="${item.orderId}" title="复制订单编号" aria-label="复制订单编号"></button></span></div>
            <div><span class="record-copy-value">${item.roundId} <button class="profit-copy" type="button" data-copy="${item.roundId}" title="复制牌局编号" aria-label="复制牌局编号"></button></span></div>
            <div>${item.gameCn}</div>
            <div>${item.gameEn}</div>
            <div>${item.trigger}</div>
            <div>${item.rtp}</div>
            <div class="record-multiple${item.multiple === "0.00x" ? " muted" : ""}">${item.multiple}</div>
            <div class="profit-num">${item.bet}</div>
            <div class="profit-num ${moneyNumber(item.win) >= 0 ? "pos" : "neg"}">${item.win}</div>
            <div class="profit-num">${item.before}</div>
            <div class="profit-num ${moneyNumber(item.after) >= 0 ? "" : "neg"}">${item.after}</div>
            <div><span class="profit-date">${item.date}<br/>${item.time}</span></div>
          </div>
        `).join("")}
      `;
      bindCopyButtons(betTable);
    }

    function renderSameDevice(platformId) {
      const records = SAME_DEVICE_MAP[platformId] || [];
      deviceSummary.textContent = `共 ${records.length} 条记录`;
      deviceTable.innerHTML = `
        <div class="same-device-row header">
          <div>ID</div><div>注册时间</div><div>最后登录</div><div>总投注</div><div>总返奖</div><div>历史盈亏</div>
        </div>
        ${records.map((item) => `
          <div class="same-device-row" data-device-platform="${item.id}">
            <div><span class="profit-player">${item.id}</span></div>
            <div>${item.registeredAt}</div>
            <div>${item.lastLogin}</div>
            <div class="profit-num">${item.totalBet}</div>
            <div class="profit-num">${item.totalPayout}</div>
            <div class="profit-num ${moneyNumber(item.historyProfit) >= 0 ? "pos" : "neg"}">${item.historyProfit}</div>
          </div>
        `).join("")}
      `;
    }

    function renderGamePanel(source) {
      const thumb = GAME_ICON_MAP[source.gameEn] || ["all", "G"];
      const brandBadge = source.brand ? `<span class="detail-brand">${source.brand}</span>` : `<span class="detail-brand">-</span>`;
      detailGameTable.innerHTML = `
        <div class="account-detail-game-row header"><div>序号</div><div>品牌</div><div>中文游戏名称</div><div>英文游戏名称</div><div>注单额</div><div>注单数</div><div>总输赢</div><div>RTP</div></div>
        <div class="account-detail-game-row"><div>1</div><div>${brandBadge}</div><div>${source.gameCn || source.gameEn || "-"}</div><div>${source.gameEn || "-"}</div><div class="profit-num">${source.bet} USD</div><div>${source.count}</div><div class="profit-num ${moneyNumber(source.win) >= 0 ? "pos" : "neg"}">${source.win} USD</div><div>${source.rtp}</div></div>
      `;
    }

    function renderGrid(platformId, source) {
      const profile = PROFILE_MAP[platformId] || {};
      const sameDeviceIds = (SAME_DEVICE_MAP[platformId] || []).map((item) => item.id).filter((id) => id !== platformId);
      detailId.textContent = `[${platformId}]`;
      detailGrid.innerHTML = [
        ["会员ID", platformId],
        ["账号状态", statusHtml(profile), true],
        ["账号标签", playerTagHtml(profile), true],
        ["所属商户", profile.merchant || "ar518888"],
        ["商户ID", profile.merchantId || platformId.slice(0, 6)],
        ["当前余额", `${profile.balance || "0.00"} USD`],
        ["当天投注", `${profile.todayBet || source.bet || "0.00"} USD`],
        ["历史投注", `${profile.historyBet || profile.todayBet || source.bet || "0.00"} USD`],
        ["今日RTP", profile.todayRtp || source.rtp || "0.00%"],
        ["历史RTP", profile.historyRtp || source.rtp || "0.00%"],
        ["今日输赢", `${profile.todayWin || source.win || "0.00"} USD`],
        ["历史输赢", `${profile.historyWin || source.win || "0.00"} USD`],
        ["今日局数", profile.todayRounds || source.count || "0"],
        ["历史局数", profile.historyRounds || source.count || "0"],
        ["三日登录次数", profile.loginCount || "0"],
        ["留存天数", profile.retentionDays || "0"],
        ["注册IP", hoverListHtml(profile.registerIps || [], `${(profile.registerIps || []).length}个IP`) , true],
        ["登录IP", hoverListHtml(profile.loginIps || [], `${(profile.loginIps || []).length}个IP`) , true],
        ["同设备ID", sameDeviceIds.length ? `<button class="detail-link-btn" type="button" data-open-detail-tab="device">${sameDeviceIds.length}个账号</button>` : "无", true],
        ["创建时间", profile.createdAt || "-"],
        ["最后登录时间", profile.lastLogin || "-"]
      ].map(([label, value, html]) => (
        `<div class="account-detail-cell"><span>${label}</span><strong>${html ? value : String(value)}</strong></div>`
      )).join("");
      bindCopyButtons(detailGrid);
    }

    function open(platformId, row) {
      const source = summarizeSource(platformId, row);
      state.currentPlatformId = platformId;
      state.currentSource = source;
      renderGrid(platformId, source);
      renderGamePanel(source);
      betFrom.value = "2026-05-06";
      betTo.value = "2026-05-11";
      syncBetGames(platformId);
      renderBetRecords(platformId);
      renderSameDevice(platformId);
      switchTab("game");
      state.modal.hidden = false;
      dialog.classList.remove("is-fullscreen");
      fullscreenButton.textContent = "全屏";
      document.body.classList.add("modal-open");
    }

    state.modal.addEventListener("click", (event) => {
      if (event.target === state.modal) {
        state.modal.hidden = true;
        document.body.classList.remove("modal-open");
        return;
      }
      const tabTrigger = event.target.closest("[data-open-detail-tab]");
      if (tabTrigger) switchTab(tabTrigger.dataset.openDetailTab);
      const deviceRow = event.target.closest(".same-device-row[data-device-platform] .profit-player");
      if (deviceRow) {
        const row = event.target.closest(".same-device-row[data-device-platform]");
        if (row) open(row.dataset.devicePlatform, null);
      }
    });

    state.modal.querySelector("[data-detail-close]").addEventListener("click", () => {
      state.modal.hidden = true;
      document.body.classList.remove("modal-open");
    });

    fullscreenButton.addEventListener("click", () => {
      const fullscreen = dialog.classList.toggle("is-fullscreen");
      fullscreenButton.textContent = fullscreen ? "还原" : "全屏";
    });

    state.modal.querySelector("[data-bet-search]").addEventListener("click", () => renderBetRecords(state.currentPlatformId));
    state.modal.querySelector("[data-bet-reset]").addEventListener("click", () => {
      betGame.value = "";
      betFrom.value = "2026-05-06";
      betTo.value = "2026-05-11";
      renderBetRecords(state.currentPlatformId);
    });
    [betGame, betFrom, betTo].forEach((control) => {
      control.addEventListener("change", () => renderBetRecords(state.currentPlatformId));
    });

    detailTabs.forEach((tab) => {
      tab.addEventListener("click", () => switchTab(tab.dataset.detailTab));
    });

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && state.modal && !state.modal.hidden) {
        state.modal.hidden = true;
        document.body.classList.remove("modal-open");
      }
    });

    return { open };
  }

  function initPageBindings() {
    const modalApi = ensureModal();
    document.addEventListener("click", (event) => {
      if (event.target.closest("[data-copy]")) return;
      const playerTrigger = event.target.closest(".profit-player");
      if (!playerTrigger) return;
      const row = playerTrigger.closest(".profit-row[data-platform], .game-record-row[data-player]");
      if (!row) return;
      const platformId = row.dataset.platform || row.dataset.player;
      if (!platformId) return;
      event.preventDefault();
      event.stopImmediatePropagation();
      modalApi.open(platformId, row);
    }, true);
  }

  function shouldInit() {
    const path = (location.pathname || "").toLowerCase();
    return path.endsWith("player-game-records.html") || path.endsWith("player-today-profit-loss.html");
  }

  if (shouldInit()) {
    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", initPageBindings, { once: true });
    } else {
      initPageBindings();
    }
  }
})();
